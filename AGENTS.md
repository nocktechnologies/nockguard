# AGENTS.md — nockguard

The repo map is `CLAUDE.md` at this root. Read it first; it is pointers only.
Then read the doc for the area you are touching: `README.md` for policy keys,
audit signing, `selftest` and coverage scope; `docs/TRUST.md`,
`docs/POOL_ROUTER.md`, or `docs/design/n8761-phase0-http-listener-forward-proxy.md`
for those components. The map's "Rules that bite" section is not optional.
